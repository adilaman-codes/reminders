name: Bethany
on:
  schedule:
    - cron: '30 7 25 6 *'   # 1:00 PM IST, June 25 (one-off)
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send notification
        run: |
          curl -s ntfy.sh/Adil-brain \
            -H "Title: Bethany" \
            -d "Bethany. 🕐"
