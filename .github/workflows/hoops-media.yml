name: Make Hoops Media
on:
  schedule:
    - cron: '30 12 25 6 *'   # 6:00 PM IST, June 25 (one-off)
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send notification
        run: |
          curl -s ntfy.sh/Adil-brain \
            -H "Title: Make the Hoops Media" \
            -d "Coach Inno's back from Rwanda. Cut the footage into reels/posts/stories. 🏀🎬"
